{
  "name": "toggle-display",
  "version": "1.0.0",
  "description": "Toggle display functionality for form elements",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js",
    "version": "echo 'Version management is now handled during build'"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
